# Guide Resolver Refactor Matrix

## Status
- Scope: split the guide resolver into fact, scan, snapshot, resolve, and cases modules while keeping `zygor/guide_resolver.lua` as the stable facade.
- Policy: parity first, with only documented `approved_improvement` cases allowed to change output.
- Diagnostics: `/awp resolvercases [all|case_id]`.
- Verification status: fixture coverage is checked in; parse validation is required after every structural change; live `/awp stepdebug` verification is still required in game for representative protected families.

## Protected Families
- `|or` clusters: visible accept, visible turnin, and hidden questpossible-style clusters stay protected.
- Detached quest-title detours: helper/header suppression must still block bad detached `q` steals.
- Stale secondary quest actions: stale `turnin` and stale/already-in-log `accept` remain filtered.
- Live-confirmed handoffs: a direct quest-frame match can seed presentation when the canonical same-target seed is known stale/unavailable.
- Ready-turnin handoffs: a quest-log-ready turnin can regain presentation ownership when Zygor's pointer advances to a same-target accept too early.
- Completed handoff corridors: same-target completed quest runs can recover their talk header across more than two handoff goals, but still stop on unrelated visible facts.
- Instruction-neighbor fallbacks: `confirm`/instruction steps keep neighboring guidance support.
- Objective helper-action fallbacks: `click` and `use` remain narrow helper actions, not global header actions.
- Passive fallbacks: passive `kill` remains preferred over passive `get/collect` when both are viable.

## Case Matrix
| Case ID | Class | Old Output | New Output | Old Reason | New Reason | Old Subtext Reason | New Subtext Reason | Notes | Verification |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `visible_or_accept_cluster` | parity | `Talk to Field Marshal Brock` / `Accept Available Quest` | unchanged | implicit actionable cluster | unchanged | `alternate_generic_accept` | unchanged | Protects visible uniform `accept |or` behavior. | fixture |
| `visible_or_turnin_cluster` | parity | `Talk to Quartermaster Brevin` / `Turn In Available Quest` | unchanged | implicit actionable cluster | unchanged | `alternate_generic_turnin` | unchanged | Protects visible uniform `turnin |or` behavior. | fixture |
| `hidden_or_cluster` | parity | `Talk to Commander Althea` / `Accept Available Quest` | unchanged | `hidden_or_cluster` | unchanged | `alternate_generic_accept` | unchanged | Protects hidden `questpossible |or` path from regressing into generic non-actionable fallback. | fixture |
| `alternate_or_direct_live_match_prefers_direct_entry` | parity | direct frame/list ambiguity could select generic alternate accept | `Talk to Archivist Lyra` / `Accept 'Quest B'` | none | unchanged | none or generic alternate | `live_quest_match` | Protects the shared live-currentness context so a unique direct quest-frame match beats ambiguous list-only dialog entries. | fixture |
| `alternate_or_ambiguous_live_match_falls_back_generic` | parity | ambiguous list matches should not select a quest | unchanged | none | unchanged | `alternate_generic_accept` | unchanged | Protects against false positives by proving ambiguous live list matches still fall back to the generic alternate accept label. | fixture |
| `live_direct_accept_seed_beats_stale_first_incomplete_turnin` | approved_improvement | stale first incomplete turnin could own title despite direct live accept | `Accept 'Eye in the Sky'` / `Talk to Rexxar` | none | unchanged | stale/current-header context | `preceding_talk_header` | Approved because a direct quest-frame accept is stronger than a same-target turnin that is marked incomplete by stale guide state but is no longer offered. | fixture |
| `stale_turnin_secondary_suppressed` | parity | `Accept 'Finding a Foothold'` / `Talk to Prophet Velen` | unchanged | none | unchanged | `context_header` | unchanged | Confirms stale turnin is filtered before secondary-action selection. | fixture |
| `stale_accept_secondary_suppressed` | parity | `Turn in 'Go to the Front'` / `Talk to Field Marshal Brock` | unchanged | none | unchanged | `context_header` | unchanged | Confirms already-in-log accept does not linger as stale secondary subtext. | fixture |
| `actionable_accept_click_helper` | approved_improvement | `Accept 'Defenstrations'` / none | `Accept 'Defenstrations'` / `Click Defense Pylon Central Control Console` | none | unchanged | none | `actionable_helper_action_fallback` | Approved because the stale turnin is correctly suppressed and the nearby click interaction is the strongest remaining actionable context. | fixture |
| `actionable_accept_click_helper_bridged_complete` | approved_improvement | `Accept 'Defenstrations'` / flashes then drops to none after turnin completion | `Accept 'Defenstrations'` / `Click Defense Pylon Central Control Console` | none | unchanged | none | `actionable_helper_action_fallback` | Approved because a completed same-target turnin should be bridgeable context, not a hard stop that hides the nearby click helper. | fixture |
| `stale_completed_title_promotes_primary_action` | approved_improvement | `Turned in 'The Exarch Council'` / `Accept 'Hataaru, the Artificer'` | `Accept 'Hataaru, the Artificer'` / `Talk to Exarch Othaar` | none | unchanged | `secondary_action` | `preceding_talk_header` | Approved because once the stale completed quest title is no longer trusted, the remaining actionable block has a single current accept and the bridged talk header becomes the correct context. | fixture |
| `stale_block_title_prefers_current_goal_action` | approved_improvement | `Turned in 'The Clarity Elixir'` / `Talk to Prophet Velen` | `Accept 'The Fate of Karabor'` / `Talk to Prophet Velen` | none | unchanged | `context_header` | unchanged | Approved because when the raw arrow title no longer matches any fact in the resolved actionable block, the current actionable goal inside that block is the strongest stable title to present. | fixture |
| `stale_completed_block_title_bridges_guidance_to_talk_header` | approved_improvement | `Accept 'The Fate of Karabor'` / none | `Accept 'The Fate of Karabor'` / `Talk to Prophet Velen` | none | unchanged | none | `preceding_talk_header` | Approved because lightweight tooltip guidance between a completed same-target handoff and its talk header should not block the existing completed-quest bridge from recovering the NPC interaction context. | fixture |
| `multi_accept_block_prefers_header_context` | approved_improvement | `Accept 'Naielle, The Rangari'` / `Accept 'Hataaru, the Artificer'` | `Accept 'Naielle, The Rangari'` / `Talk to Exarch Othaar` | none | unchanged | `secondary_action` | `preceding_talk_header` | Approved because in multi-accept interaction blocks the shared NPC interaction context is stronger and more stable than presenting a sibling accept as subtext. | fixture |
| `split_multi_accept_block_recovers_talk_header` | approved_improvement | `Accept 'The Great Salvation'` / none | `Accept 'On the Offensive'` / `Talk to Rangari Saa'to` | none | unchanged | none | `context_header` | Approved because same-action canonical selection should hand the block back to the earliest remaining accept, letting the direct talk header own context again. | fixture |
| `presentation_current_action_owns_title_over_sibling_raw_match` | approved_improvement | stale raw sibling title could stay on `Turn in 'Quest A'` | `Accept 'Quest B'` / `Talk to Prophet Velen` | none | unchanged | none or stale sibling context | `context_header` | Protects the shared presentation owner so mixed blocks keep title ownership on the selected current actionable fact even when raw title still matches a sibling in the block. | fixture |
| `presentation_bridged_completed_corridor_promotes_primary_title_owner` | approved_improvement | stale completed raw title could stay on `Turned in 'Quest A'` | `Accept 'Quest B'` / `Talk to Prophet Velen` | none | unchanged | none | `preceding_talk_header` | Protects the title-owner corridor rule so a bridgeable completed quest before the live block promotes the primary actionable title instead of preserving the stale completed raw title. | fixture |
| `presentation_direct_header_beats_corridor_fallback` | approved_improvement | bridged earlier talk could steal context even when a direct local header exists | `Accept 'Quest B'` / `Talk to Archmage Khadgar` | none | unchanged | `preceding_talk_header` or none | `context_header` | Protects the unified header resolver ordering: direct header context around the live block wins before corridor fallback to an earlier talk header. | fixture |
| `presentation_corridor_recovers_preceding_talk_header` | approved_improvement | none | `Accept 'Quest B'` / `Talk to Rangari Arepheon` | none | unchanged | none | `preceding_talk_header` | Protects the unified corridor walk so a single completed same-target bridge fact still recovers the preceding talk header when no direct header exists. | fixture |
| `presentation_corridor_stops_on_broken_bridge` | approved_improvement | false-positive earlier talk recovery possible | `Accept 'Quest B'` / none | none | unchanged | false-positive `preceding_talk_header` | none | Protects the unified corridor walk from crossing an unrelated visible fact that breaks the bridge context before the earlier talk header. | fixture |
| `completed_three_turnin_corridor_recovers_talk_header` | approved_improvement | `Accept 'Eye in the Sky'` / none | `Accept 'Eye in the Sky'` / `Talk to Rexxar` | none | unchanged | none | `preceding_talk_header` | Approved because multi-turnin NPC handoffs remain one contiguous same-target interaction even when three completed turnins sit between the accept and the talk header. | fixture |
| `completed_turnin_corridor_stops_on_unrelated_visible_fact` | parity | false-positive earlier talk recovery would be possible after removing the fixed two-turnin cap | unchanged | none | unchanged | none | unchanged | Protects the broader completed-corridor rule from crossing unrelated visible facts. | fixture |
| `ready_turnin_before_accept_seed_prefers_turnin` | approved_improvement | Zygor proximity could advance presentation to `Accept 'Mercy for the Living'` while a prior ready turnin remained incomplete | `Turn in 'Vengeance for the Fallen'` / `Talk to Khaano` | none | unchanged | `preceding_talk_header` | `context_header` | Approved because quest-log readiness is stronger evidence than a premature same-target accept pointer when preserving guide handoff order. | fixture |
| `ready_turnin_before_accept_seed_keeps_accept_without_ready_proof` | parity | stale earlier turnins must not steal an accept | unchanged | none | unchanged | `preceding_talk_header` | unchanged | Protects against regressing stale-turnin behavior by requiring quest-log or live-dialog readiness proof before the mixed handoff correction fires. | fixture |
| `ready_turnin_before_accept_seed_stops_on_unrelated_visible_fact` | parity | false-positive turnin recovery could cross unrelated visible steps | unchanged | none | unchanged | none | unchanged | Protects the ready-turnin correction from crossing broken same-target corridors. | fixture |
| `mixed_turnin_accept_block_prefers_header_context` | approved_improvement | `Turn in 'Quest A'` / `Accept 'Quest B'` | `Turn in 'Quest A'` / `Talk to Field Marshal Brock` | none | unchanged | `secondary_action` | `context_header` | Approved because mixed same-target quest blocks are still a single interaction, and the talk header better describes the interaction than a sibling quest action. | fixture |
| `multi_turnin_block_prefers_header_context` | approved_improvement | `Turn in 'Quest A'` / `Turn in 'Quest B'` | `Turn in 'Quest A'` / `Talk to Quartermaster Brevin` | none | unchanged | `secondary_action` | `context_header` | Approved because multi-turnin blocks should present the interaction target as context instead of another sibling turnin quest. | fixture |
| `nearby_turnin_accept_shared_header_bridge` | approved_improvement | `Accept 'The Clarity Elixir'` / none | `Accept 'The Clarity Elixir'` / `Talk to Prophet Velen` | none | unchanged | none | `context_header` | Approved because adjacent quest actions with tiny coordinate drift should still merge when a shared interaction header clearly anchors them to one NPC interaction. | fixture |
| `nearby_turnin_accept_no_header_no_bridge` | parity | `Accept 'Quest B'` / none | unchanged | none | unchanged | none | unchanged | Protects against false positives by proving nearby coordinate drift alone does not merge quest actions without shared interaction/header context. | fixture |
| `stale_completed_anchor_shared_header_seed_bridge` | approved_improvement | `Turned in 'Into Twilight'` / none | `Accept 'The Clarity Elixir'` / `Talk to Prophet Velen` | `no_same_target_action` | none | none | `preceding_talk_header` | Approved because a stale completed quest anchor should still seed the nearby current quest action when a shared interaction header clearly proves the same NPC handoff. | fixture |
| `completed_turnin_bridge_recovers_talk_header_with_drift` | approved_improvement | `Accept 'Gestating Genesaur'` / none | `Accept 'Gestating Genesaur'` / `Talk to Rangari Arepheon` | none | unchanged | none | `preceding_talk_header` | Approved because completed same-target turnins with tiny coordinate drift should follow the same guarded local bridge policy as seed and block expansion, allowing the accept to recover its talk header context. | fixture |
| `stale_completed_anchor_no_header_no_seed_bridge` | parity | no snapshot | unchanged | `no_same_target_action` | unchanged | none | unchanged | Protects against false positives by proving stale completed anchors still do not bridge on coordinate drift alone without shared interaction/header context. | fixture |
| `instruction_neighbor_confirm` | parity | `Click Here Once Baron Alexston Arrives` / nearby wait guidance | unchanged | `instruction_neighbor_guidance` | unchanged | `instruction_neighbor_guidance` | unchanged | Preserves `confirm` instruction-neighbor fallback. | fixture |
| `talk_invehicle_instruction_neighbor` | approved_improvement | `Talk to Dungar Longdrink` / none | `Talk to Dungar Longdrink` / `Take a Flight to Embaari Village, Shadowmoon Valley` | `no_visible_actionable_goal` | `instruction_neighbor_action` | none | `instruction_neighbor_action` | Approved because the NPC remains the routed target while the flight instruction is the correct contextual subtext. | fixture |
| `q_objective_text_fallback` | approved_improvement | `Defend the Camp` / none | `Defend the Camp` / `Kill the enemies that attack` | `no_visible_actionable_goal` | `non_actionable_objective_text_fallback` | none | `non_actionable_objective_text_fallback` | Approved because nearby objective text is a stronger instruction than the generic caution tip for non-actionable `q` objectives. | fixture |
| `get_objective_passive_kill_preferred_over_guidance` | approved_improvement | `Collect 1000 Raw Elekk Steaks` / `They look like elephants.` | `Collect 1000 Raw Elekk Steaks` / `Kill Rockhide enemies` | `non_actionable_objective_guidance_fallback` | `passive_kill_fallback` | `non_actionable_objective_guidance_fallback` | `passive_kill_fallback` | Approved because for non-actionable collection objectives, nearby passive kill context explains the actual acquisition method better than a generic flavor tip. | fixture |
| `objective_helper_cluster_prefers_setup_guidance` | approved_improvement | `Complete the Elixir` / `Click Moonlit Herb` | `Complete the Elixir` / `You can reach all of the ingredients from this location.` | `non_actionable_helper_action_fallback` | `non_actionable_objective_guidance_fallback` | `non_actionable_helper_action_fallback` | `non_actionable_objective_guidance_fallback` | Approved because multi-helper ingredient clusters are better summarized by the setup guidance immediately before the helper run than by one specific ingredient click. | fixture |
| `objective_interaction_chain_promotes_talk_title` | approved_improvement | `Obtain an Orc Disguise From Sylene` / `Select "I need a Shadowmoon orc illusion."` | `Talk to Sylene` / `Select "I need a Shadowmoon orc illusion."` | `non_actionable_header_fallback` | `non_actionable_interaction_chain_fallback` | `non_actionable_header_fallback` | `non_actionable_interaction_chain_fallback` | Approved because a primary NPC interaction followed by a gossip choice is clearer presented as interaction title plus gossip subtext than as a raw objective title plus gossip subtext. | fixture |
| `objective_interaction_chain_bridges_text_to_gossip` | approved_improvement | `Escort Your Garrison Army to Karabor` / `Kill enemies around this area` | `Talk to Vindicator Maraad` / `Select "I am ready to join the attack against the Iron Horde."` | `non_actionable_objective_text_fallback` | `non_actionable_interaction_chain_fallback` | `non_actionable_objective_text_fallback` | `non_actionable_interaction_chain_fallback` | Approved because informational text and lightweight tooltip guidance between the `q` seed and gossip choice should not block the existing talk/gossip interaction-chain promotion. | fixture |
| `scenario_goal_prefers_objective_text_subtext` | approved_improvement | `Defend K'ara Until it Becomes Fully Empowered` / none | `Defend K'ara Until it Becomes Fully Empowered` / `Kill the enemies that attack in waves` | `no_visible_actionable_goal` | `non_actionable_objective_text_fallback` | none | `non_actionable_objective_text_fallback` | Approved because incomplete `scenariogoal` objectives are still non-actionable scenario steps, and nearby objective text is the strongest contextual instruction. | fixture |
| `scenario_end_prefers_passive_kill_subtext` | approved_improvement | `Defeat Commander Vorka` / none | `Defeat Commander Vorka` / `Kill Commander Vorka` | `no_visible_actionable_goal` | `passive_kill_fallback` | none | `passive_kill_fallback` | Approved because incomplete `scenarioend` steps are boss-finish objectives, and nearby passive kill context is stronger than generic tooltip guidance. | fixture |
| `scenario_start_click_helper` | approved_improvement | `Begin the Daggerspine Point Scenario` / none | `Begin the Daggerspine Point Scenario` / `Click Curious Obelisk` | `no_visible_actionable_goal` | `non_actionable_helper_action_fallback` | none | `non_actionable_helper_action_fallback` | Approved because `scenariostart` marks where to begin a scenario and a nearby click/use interaction is the correct setup instruction before the scenario triggers. | fixture |
| `objective_gossip_only_keeps_objective_title` | parity | `Obtain an Orc Disguise From Sylene` / gossip subtext | unchanged | `non_actionable_header_fallback` | unchanged | `non_actionable_header_fallback` | unchanged | Protects existing behavior when there is no preceding primary interaction header to justify promoting a talk/click title. | fixture |
| `detached_q_suppressed_by_gossip` | parity | `Talk to Shelly Hamby` / `Select "Gather Shelly's report."` | unchanged | `non_actionable_header_fallback` | unchanged | `non_actionable_header_fallback` | unchanged | Keeps detached `q` from stealing presentation when a better local helper/header exists. | fixture |
| `detached_warning_q_clicknpc` | approved_improvement | `Click Architect Table` / helper text or weak context | `Build Your Barracks` / `Click Architect Table` | weak non-actionable fallback | `detached_quest_title_fallback` | weak helper/header fallback | `context_header` | Approved because the quest objective is the real destination meaning and the click action is the correct context subtext. | fixture |
| `detached_accept_title_detour` | approved_improvement | `Talk to Field Marshal Brock` / none or weak context | `Accept 'Disruption - Forge Camp: Mageddon'` / `Talk to Field Marshal Brock` | weak/no same-target route presentation | `detached_quest_title_fallback` | weak or missing | `context_header` | Approved because detached accept quest context is the real presentation target once the live interaction and location are known. | fixture |
| `detached_turnin_title_detour` | approved_improvement | `Talk to Baros Alexston` / none or weak context | `Turn in 'Establish Your Garrison'` / `Talk to Baros Alexston` | weak/no same-target route presentation | `detached_quest_title_fallback` | weak or missing | `context_header` | Approved because detached turnin quest context should outrank the generic NPC name when live dialog confirms the turnin is current. | fixture |
| `detached_turnin_title_detour_without_live` | approved_improvement | `Talk to Baros Alexston` / none | `Turn in 'Supply Drop'` / `Talk to Baros Alexston` | `no_same_target_action` | `detached_quest_title_fallback` | none | `context_header` | Approved as a last-chance actionable fallback because the step still has a single clear detached turnin candidate attached to the active NPC target even though live dialog evidence is absent. | fixture |
| `detached_turnin_accept_priority_without_live` | approved_improvement | `Talk to Baros Alexston` / none or wrong detached accept | `Turn in 'Supply Drop'` / `Talk to Baros Alexston` | `no_same_target_action` | `detached_quest_title_fallback` | none or wrong detached secondary | `context_header` | Approved because the looser detached fallback still preserves existing quest priority: current turnin outranks current accept when both are detached and no same-target actionable fact exists. | fixture |
| `get_click_helper` | approved_improvement | `Collect Draenei Bucket (0/1)` / none | `Collect Draenei Bucket (0/1)` / `Click Draenei Buckets` | `no_visible_actionable_goal` or weak fallback | `non_actionable_helper_action_fallback` | none | `non_actionable_helper_action_fallback` | Approved because nearby click guidance is the missing actionable context for the collect objective. | fixture |
| `q_click_helper` | approved_improvement | `Find the Blueprints` / none | `Find the Blueprints` / `Click Garrison Blueprint: Barracks` | `no_visible_actionable_goal` or weak fallback | `non_actionable_helper_action_fallback` | none | `non_actionable_helper_action_fallback` | Approved because nearby click guidance is the right narrow helper action for the quest objective without broadening click into header selection. | fixture |
| `q_use_helper` | approved_improvement | `Douse the Bookshelf Fire` / none | `Douse the Bookshelf Fire` / `Use the Draenei Bucket` | `no_visible_actionable_goal` or weak fallback | `non_actionable_helper_action_fallback` | none | `non_actionable_helper_action_fallback` | Approved because nearby use guidance is the correct helper action for the quest objective. | fixture |
| `havebuff_click_helper` | approved_improvement | `Carry the Crystal` / none | `Carry the Crystal` / `Click Charged Resonance Crystal` | `no_visible_actionable_goal` or weak fallback | `non_actionable_helper_action_fallback` | none | `non_actionable_helper_action_fallback` | Approved because `havebuff` is another objective-style seed and the nearby click action is the correct setup instruction. | fixture |
| `passive_kill_preferred_over_get` | approved_improvement | `Accept 'Disruption - Forge Camp: Mageddon'` / weak or missing collect subtext | `Accept 'Disruption - Forge Camp: Mageddon'` / `Kill Daetan Swiftplume` | none | unchanged | weak passive get fallback or none | `passive_kill_fallback` | Approved because passive `kill` is the stronger gameplay instruction than passive `get/collect` here. | fixture |
| `passive_get_fallback` | approved_improvement | `Accept 'Disruption - Forge Camp: Mageddon'` / none | `Accept 'Disruption - Forge Camp: Mageddon'` / `Collect Noblegarden Trinket (0)` | none | unchanged | none | `passive_get_fallback` | Approved because passive `get/collect` remains the correct late fallback when no stronger passive kill exists. | fixture |
| `carrier_leg_suppressed` | parity | quest-derived route-leg subtext suppressed | unchanged | none | unchanged | `carrier_leg_suppressed` | unchanged | Protects the carrier-leg suppression contract for quest-derived route presentation. | fixture |
| `canonical_scenariogoal_completed_stage_advances_to_next_objective` | approved_improvement | stale completed scenario stage could keep ownership as `Clear a Path to Karabor Harbor` | `Meet Yrel at the Karabor Harbor` / none | `no_visible_actionable_goal` or stale bypassed route fallback | `current_goal_non_actionable_title` | none | unchanged | Approved because same-target scenario-stage handoffs should advance structurally to the next incomplete scenariogoal, and once that happens the current incomplete scenario objective should own the non-actionable title instead of the stale completed stage. | fixture |
| `canonical_middle_complete_accept_selects_first_remaining` | approved_improvement | resolver anchors to raw stale goal | `Accept 'Quest A'` / `Talk to Warden Thelassa` | none | unchanged | none | `context_header` | Verifies resolver correctly anchors to canonical (first incomplete) accept when canonical pipeline provides it. | fixture |
| `canonical_middle_complete_turnin_selects_first_remaining` | approved_improvement | resolver anchors to raw stale goal | `Turn in 'Quest A'` / `Talk to Overseer Vinara` | none | unchanged | none | `context_header` | Same as above for turnin cluster. | fixture |
| `canonical_earliest_completes_advances_to_next` | approved_improvement | resolver anchors to raw (now complete) first goal | `Accept 'Quest B'` / `Talk to Sergeant Aldric` | none | unchanged | none | `preceding_talk_header` | Verifies canonical goal advances past completed earliest sibling to next incomplete while header recovery still uses the guarded completed-quest bridge fallback. | fixture |
| `canonical_nearby_drift_without_bridge_no_override` | parity | `Accept 'Quest B'` / none | unchanged | none | unchanged | none | unchanged | Protects against cluster formation on coord drift alone without bridge context. No override fires. | fixture |
| `canonical_raw_later_incomplete_not_earliest` | approved_improvement | resolver anchors to raw later incomplete | `Accept 'Quest A'` / `Talk to Spiritwalker Yalna` | none | unchanged | none | `context_header` | Verifies canonical fires even when raw goal is incomplete but not the earliest incomplete in cluster. | fixture |
| `canonical_all_cluster_goals_complete_no_override` | parity | no snapshot (non-actionable) | unchanged | `no_visible_actionable_goal` | unchanged | none | unchanged | Verifies canonical selector does not fire when entire cluster is complete. Raw passthrough, non-actionable fallback. | fixture |
| `visible_kill_goal_keeps_quest_semantics` | approved_improvement | visible guide kill destination could fall through with no snapshot and render as generic guide/provider icon | `Kill Witch Lord Morkurk` / none, with `semanticKind=quest` and `semanticQuestID=34980` | `no_visible_actionable_goal` | `visible_quest_goal_semantic_fallback` | none | unchanged | Approved because a visible incomplete quest goal should retain quest semantics even when there is no actionable subtext or guide button presentation. | fixture |
| `canonical_solo_goal_no_cluster_no_override` | parity | `Accept 'Quest A'` / `Talk to Healer Moswen` | unchanged | none | unchanged | `context_header` | unchanged | Verifies no cluster forms for solo goals; canonical selector passes through; resolver works normally. | fixture |

## Notes
- Legacy debug/subtext reasons are preserved through explicit mapping tables in `zygor/guide_resolver/cases.lua`.
- The fixture runner reports both normalized reasons and legacy-mapped reasons so diagnostics remain readable during refactor cleanup.
- Canonical goal selection is intentionally scoped to same-action quest handoff clusters. Mixed `turnin`/`accept` blocks stay on the existing resolver and stale-secondary policy paths; live direct seed correction and quest-log-ready turnin correction are applied later during actionable presentation selection.
- The current checked-in suite is focused on the protected and recently changed families. Additional cases should be added before broadening behavior outside those families.
